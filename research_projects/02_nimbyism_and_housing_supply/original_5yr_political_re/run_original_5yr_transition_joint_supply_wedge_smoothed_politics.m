function [summary, results] = run_original_5yr_transition_joint_supply_wedge_smoothed_politics(varargin)
% Backward-compatible wrapper that forwards to the short canonical entry point.
[summary, results] = run_original_5yr_joint_wedge_smoothed(varargin{:});
end
